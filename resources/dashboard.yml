resources:
  dashboards:
    painel_academico:
      display_name: "[${bundle.target}] Painel Acadêmico"
      file_path: ../src/dashboards/painel_academico.lvdash.json
      warehouse_id: ${var.warehouse_id}
